name: Tests

on: 
  push:
    paths-ignore:
      - "*.md"

env:
  CARGO_TERM_COLOR: always

jobs:    

  tests-ubuntu:
    runs-on: ubuntu-latest
    steps:
    - uses: ./.github/workflows/actions/prepare_ubuntu
    - name: Run tests
      run: cargo test
